import type { JSX } from "solid-js";

export const AnimateFadeInUpBig = ({ children }: { children: JSX.Element }) => {
	return <div class="animate__animated animate__fadeInUpBig">{children}</div>;
};
